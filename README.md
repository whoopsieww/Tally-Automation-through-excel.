# Tally-Automation-through-excel.
When the code is running, it will search a pdf named "test.pdf" inside the same folder, and extract its content and put into an excel sheet, Blank excel sheet needs to be opened for this, after that, run the VBA code in that sheet to generate a xml file in the same folder, import this file into Tally to make automatic entries
